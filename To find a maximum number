i = 2
a_max = a_1

for i 
  if a_max <= a_i
    a_max = a_i
  else
  
  
  #if want to find two largest numbers 
  
  i = 2
  a_max = a_1
  a_max_next = a_1
  
  for i
    if a_max <= a_i
      a_max_next = a_max
      a_max = a_i
    else
      
